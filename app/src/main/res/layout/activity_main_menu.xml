<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="com.band.guy.kool.connect4.MainMenu"
    android:background="@drawable/background">


    <ImageButton
        android:layout_width="200dp"
        android:layout_height="117dp"
        android:id="@+id/btnLocal"
        android:onClick="OnClick"
        android:src="@drawable/btn2joueur"
        android:layout_alignTop="@+id/btnHote"
        android:layout_alignParentStart="true" />

    <ImageButton
        android:layout_width="200dp"
        android:layout_height="117dp"
        android:id="@+id/btnHote"
        android:onClick="OnClick"
        android:src="@drawable/btnvsai"
        android:layout_centerVertical="true"
        android:layout_alignParentEnd="true" />

    <ImageButton
        android:layout_height="117dp"
        android:layout_width="200dp"
        android:id="@+id/btnRejoindre"
        android:onClick="OnClick"
        android:src="@drawable/btngravity"
        android:layout_alignTop="@+id/btnLocal"
        android:layout_centerHorizontal="true" />

    <TextView
        android:layout_width="600dp"
        android:layout_height="wrap_content"
        android:textAppearance="?android:attr/textAppearanceLarge"
        android:text="KGB CONNECT 4 Gravity Edition"
        android:id="@+id/title"
        android:editable="true"
        android:textColor="#010101"
        android:textSize="@dimen/abc_action_button_min_width_overflow_material"
        android:layout_alignParentTop="true"
        android:layout_centerHorizontal="true" />
</RelativeLayout>
